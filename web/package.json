{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node -e \"const fs=require('fs'),path=require('path'),{execSync}=require('child_process');const lp=path.join('.next','dev','lock');if(fs.existsSync(lp))fs.rmSync(lp,{force:true});try{const r=execSync('netstat -ano | findstr :3000',{encoding:'utf8'});const pid=[...new Set(r.trim().split('\\n').map(l=>l.trim().split(/\\s+/).pop()).filter(p=>p&&p!=='0'))];pid.forEach(p=>{try{execSync('taskkill /PID '+p+' /F',{stdio:'ignore'})}catch(_){}});if(pid.length)console.log('[dev] Freed port 3000 (PID '+pid.join(',')+').');}catch(_){}\" && next dev -p 3000",
    "build": "next build",
    "start": "next start",
    "lint": "eslint",
    "test": "vitest"
  },
  "dependencies": {
    "@types/katex": "^0.16.8",
    "@types/nodemailer": "^7.0.11",
    "date-fns": "^4.1.0",
    "jspdf": "^4.2.0",
    "jspdf-autotable": "^5.0.7",
    "katex": "^0.16.37",
    "lucide-react": "^0.575.0",
    "next": "^16.1.6",
    "nodemailer": "^8.0.3",
    "react": "19.2.3",
    "react-dom": "19.2.3",
    "react-icons": "^5.5.0",
    "recharts": "^3.7.0",
    "socket.io-client": "^4.8.3",
    "zustand": "^5.0.11"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@testing-library/jest-dom": "^6.6.3",
    "@testing-library/react": "^16.3.0",
    "@types/jspdf": "^1.3.3",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "@vitest/ui": "^2.1.9",
    "eslint": "^9",
    "eslint-config-next": "16.1.6",
    "fast-check": "^3.22.0",
    "jsdom": "^25.0.1",
    "tailwindcss": "^4",
    "typescript": "^5",
    "vitest": "^2.1.9"
  }
}
